apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: llama-server
    patch: |
      - op: replace
        path: /spec/replicas
        value: {{ llama_replicas | default(3) }}
