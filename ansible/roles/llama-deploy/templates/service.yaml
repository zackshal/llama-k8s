apiVersion: v1
kind: Service
metadata:
  name: llama-service
  namespace: llama
spec:
  selector:
    app: llama-server
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
